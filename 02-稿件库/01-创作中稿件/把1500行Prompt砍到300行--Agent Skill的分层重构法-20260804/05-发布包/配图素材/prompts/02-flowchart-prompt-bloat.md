---
illustration_id: 02
type: flowchart
style: sketch-notes
palette: macaron
aspect_ratio: "16:9"
---

# 代码要分层 → Prompt 也得分层 — 衔接流程

Hand-drawn educational sketchnote flowchart on warm cream paper. Black
hand-drawn lines with slight wobble, soft pastel step blocks. Diagram-style
visuals ONLY — no realistic or photographic images.

PALETTE: macaron — soft pastel blocks on warm cream.
COLORS: Warm Cream background (#F5F0E8); Black (#1A1A1A) for ALL outlines,
text, arrows, doodles; step-card fills in Light Blue (#A8D8EA), Lavender
(#D5C6E0), Peach (#FFD5C2); Coral Red (#E8655A) reserved for the looping
"bloat / loop-back" arrow and the final "也得分层重构" forward emphasis; Warm
Gray (#6B6B6B) for small annotations.
Color values (#hex) and color names are rendering guidance only — do NOT
display color names, hex codes, or palette labels as visible text in the image.

LAYOUT (left -> right main flow of 4 step cards, with a big curved loop-back
arrow arcing over the top from the last step back toward the first):
Four rounded-rectangle step cards in a horizontal row, connected by thick
hand-drawn arrows. A large Coral Red curved "loop-back" arrow arcs over the
top from step 3 back toward step 1, labeled to show the vicious cycle. A final
bold forward arrow exits the right edge toward the next concept (four-layer
architecture).

STEPS:
1. Card 1 (Light Blue): "大代码文件" — icon: a thick stack of documents / one
   oversized file. Small sub-label "几千行 py" and tiny tag "flask_web_server.py".
2. Card 2 (Lavender): "让 AI 评估" — icon: a small AI brain / gear. Arrow-in
   label "喂给 AI".
3. Card 3 (Peach): "提示词也膨胀" — icon: a document/balloon bulging fatter.
   Small tag "越写越长".
4. Card 4 (Coral Red accent border): forward exit — "也得分层重构" — icon: a
   small four-layer stack pointing onward.

CONNECTIONS:
- Solid hand-drawn arrows between cards 1 → 2 → 3 → 4 (left to right).
- A big curved Coral Red arrow looping from card 3 back over the top toward
  card 1, with the hand-lettered inline label "越强调越长 · 绕回原点".
- The final forward arrow (bold, Coral Red) exits the right edge, labeled
  "也得分层重构 →".

LABELS (hand-lettered, Chinese, large & prominent):
- Title (top, oversized): "代码分层 → Prompt 也得分层"
- Card labels: "大代码文件", "让 AI 评估", "提示词也膨胀", "也得分层重构"
- Loop label: "越强调越长 · 绕回原点"
- Bottom takeaway (single hand-lettered line): "卡脖子的是 Prompt 的结构"

STYLE: Minimal, airy. Color fills do not completely fill outlines (slight
hand-painted overshoot). ALL text hand-lettered — no computer fonts. Short
keywords only. Generous white space between cards. The loop-back arrow is the
visual emphasis (Coral Red, bold).
Text should be large and prominent with handwritten-style fonts. Keep minimal,
focus on keywords.
Clean composition with generous white space. Simple background.

ASPECT: 16:9
